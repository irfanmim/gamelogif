/* Fakta */
/** NPC yang dapat diajak berbicara**/
talk(warga).
talk(teroris).
talk(mantan).
talk(zookeeper).
talk(satpam).
talk(mertua).

/** Item yang dapat diambil **/

/*** Item pada daerah "Gudang" ***/
item(riffle).
item(shotgun).
item(handgun).
item(gasairmata).
item(basoka).

/*** Item pada daerah "Warteg" ***/
item(ikan).
item(nasipadang),
item(keepsi).
item(teh).
item(airputih).

/*** Item pada daerah "Rumah" ***/
item(dompet).
item(bunga).

/*** Item pada daerah ***/
item(adt).

/* Rules */
